import React, { useRef, useEffect } from "react";
const ModalBackdrop = ({ openState }) => {

return <div className={isOpen ? "modal_backdrop active" : "modal_backdrop"}></div>
};
export default ModalBackdrop;